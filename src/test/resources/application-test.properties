# 통합 테스트 프로파일 (@ActiveProfiles("test"))
# DataSource는 TestInfraConfig의 Testcontainers PostgreSQL(@ServiceConnection)이 대체한다.
spring.jpa.hibernate.ddl-auto=create-drop

# 외부 인프라 차단
app.rabbitmq.enabled=false
management.health.rabbit.enabled=false
spring.cache.type=simple

# 외부 API 더미 값
openai.api.key=test-key
livekit.url=ws://localhost:7880
livekit.api-key=devkey
livekit.api-secret=test-livekit-secret-0123456789abcdef

cloud.aws.credentials.accessKey=test
cloud.aws.credentials.secretKey=test
cloud.aws.region.static=us-east-1
cloud.aws.s3.bucket=test-bucket
cloud.aws.s3.endpoint=
